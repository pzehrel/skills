interface:
  display_name: "多 Agent 开发编排"
  short_description: "自主拆解任务并管理父子分支、worktree 及合并后回收"
  default_prompt: "使用 $multiple-agent-develop 从主分支编排多 Agent 开发，并在我明确命令合入主分支后清理本次父子分支和 worktree。"

policy:
  allow_implicit_invocation: false
